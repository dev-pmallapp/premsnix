{
  # Deprecated legacy template (khanelisnow) removed; retained as empty stub.
  description = "Deprecated snowfall template (rebranded)";

  inputs = {
    # Intentionally stripped
  };

  outputs = _inputs: { };
}
